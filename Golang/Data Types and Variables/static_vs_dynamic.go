package main

import "fmt"

func main(){
	name:= "Lisa"  // Short variable declaration. No need to specify the type as Go will automatically assign this. 
	// Therefore, it feels like a dynamicly typed language but is actually statically typed.
	fmt.Println(name)
}