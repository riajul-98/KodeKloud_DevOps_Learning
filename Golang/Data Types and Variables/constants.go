package main
import "fmt"

func main(){
	const age = 12 		// Untyped constant (no data type declared), more flexible.
	const f_name, age = "Riajul", 26

	const name string = "Riajul"		// Typed constants

	// Constant values must be assigned at declaration. Cannot assign value afterwards.
}